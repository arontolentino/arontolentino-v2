export default function Popular() {
  return (
    <div className="space-y-3">
      <p className="text-lg font-semibold">Popular Posts</p>
      <div className="flex flex-wrap">
        {/* {categories.map(({ name, color }) => (
          <Badge key={name} color={color} name={name} className="mb-2 mr-2" />
        ))} */}
      </div>
    </div>
  );
}
